{
  "name": "abd-monorepo",
  "version": "0.1.0",
  "private": true,
  "description": "Advanced Browser Detector — identify the real browser behind a spoofed user-agent",
  "license": "MIT",
  "workspaces": ["packages/*", "apps/*"],
  "trustedDependencies": ["esbuild", "@tailwindcss/oxide"],
  "scripts": {
    "build": "bun run --filter '@abd/core' build && bun run --filter '@abd/cli' build",
    "dev:web": "bun run --filter '@abd/web' dev",
    "abd": "bun packages/cli/src/index.ts",
    "serve": "bun packages/cli/src/index.ts serve",
    "tls": "bun run --filter '@abd/cli' build && node packages/cli/dist/index.js tls",
    "registry": "bun run --filter '@abd/web' registry",
    "test": "bun run build && bun run --filter '*' test",
    "typecheck": "bun run --filter '*' typecheck"
  },
  "devDependencies": {
    "typescript": "^5.6.3"
  }
}
